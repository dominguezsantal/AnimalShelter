@{
  Layout = "_Layout";
}

@using Inventory.Models;
@model List<Inventory.Models.PetCollection>;

<h1> Pet Collection</h1>

@if (@Model.Count == 0)
{
  <h3>No pet collection have been added yet!</h3>
} 

@foreach (PetCollection petCollection in Model)
{
  <li>@Html.ActionLink($"{pet.Name}", "Details", new { id = pet.petCollectionId })</li>
}

<p>@Html.ActionLink("Add new pet", "Create")</p>

<p>@Html.ActionLink("Home", "Index", "Home")</p>